

//Arrow Function: funcitons having arrow sign

let add = (a:number,b:number) => a + b
console.log(add(2,4));

// Annonyous function: Function declared without function keyword

const sum = function(a: number, b: number) {
    return a + b;
}
console.log(sum(4,6));
